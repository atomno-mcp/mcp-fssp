"""mcp-fssp — MCP-сервер для проверки задолженностей через ФССП.

Публичный контракт пакета — набор async-тулзов, регистрируемых в `server.py`.
Версия фиксируется здесь и читается в `pyproject.toml` при публикации.
"""

__version__ = "0.1.2"
